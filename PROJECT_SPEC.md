# InfraTutor V0.1 产品规格

## 1. 产品愿景

InfraTutor 希望把高速互联部门的新人成长过程，从“自己翻培训资料、问同事、机械跑脚本”变成一个可观测、可适应、能形成证据闭环的 AI 教学过程。

长期形态：

```text
诊断基础
  ↓
生成学习路径
  ↓
讲解 + 提问 + 实践
  ↓
结构化评估
  ↓
更新 Learner State
  ↓
回退 / 提示 / 前进 / 复习
  ↓
最终通过真实任务验证能力
```

V0.1 不试图一次解决完整培训，只验证其中最关键的自适应教学闭环。

## 2. 要解决的问题

传统新人培训常见问题：

- 文档与课程很多，但新人不知道先学什么。
- 新人不知道自己缺少哪些前置知识。
- 看完、听懂和真正会用被混为一谈。
- 老师或主管很难知道新人真正卡在哪里。
- 普通聊天机器人只会回答当前问题，不会持续维护学习状态和课程依赖。
- AI 过早给出完整答案，可能帮助完成任务，却没有促进掌握。

## 3. 目标用户

### 3.1 Primary Persona

刚毕业的计算机专业学生，正在进入高速互联、HPC 或 AI Infra 相关团队。

### 3.2 典型特征

- 对 Linux、Python、C 有课堂或项目基础。
- 能看懂常规代码，但不一定理解系统与硬件数据流。
- 对 PCIe、NUMA、HCA、RDMA、MPI、UCX、RCCL 缺乏整体框架。
- 容易在术语、抽象层次和真实运行链路之间迷失。
- 更需要“整体框架 → 实际意义 → 关键细节 → 动手验证”的教学方式。

## 4. V0.1 核心假设

> 当系统拥有人工课程知识图谱、细粒度 Learner State、基于 rubric 的 LLM 结构化评估和确定性的 Tutor Engine 时，它能比普通聊天框更准确地发现前置知识缺口，并改变下一步教学行为。

## 5. V0.1 核心目标

### G1：完整路线可见

首页展示九阶段学习路线与各阶段主要知识节点，让用户知道长期学习位置。

### G2：跑通一个自适应切片

真正可学习的范围为：

```text
Virtual / Physical Memory
          ↓
        DMA
          ↓
    Pinned Memory
          ↓
         HCA
          ↓
      Why RDMA
          ↓
    RDMA Data Path
          ↓
Memory Registration
          ↓
   lkey / rkey Intro
```

### G3：结构化评估

自由回答不能只得到一段自然语言评论；系统必须根据人工 rubric 输出可验证的结构化结果。

### G4：证据驱动状态

系统保存每个节点的掌握度、置信度、证据和误解。状态变化必须可追溯。

### G5：Tutor 控制流程

系统根据课程前置关系与学习者证据选择下一动作，而不是照搬 LLM 建议。

### G6：黄金路径可重复

使用 Mock LLM 时，Golden Path 必须在测试中稳定复现；使用真实 LLM 时，行为应在允许的语义范围内通过烟雾测试。

## 6. V0.1 非目标

以下内容明确不属于 V0.1：

- 完整九阶段课程内容实现。
- 企业登录、多人、主管后台、权限与组织管理。
- 部门内部资料检索和 RAG。
- SSH / Web Terminal。
- 自动申请 Slurm 资源。
- 自动运行 RDMA / RCCL 实验。
- 自动读取集群日志。
- 多 Agent 协作。
- Fine-tuning、自训练、模型评测平台。
- 本地模型部署。
- 移动端 App 或小程序。
- 精细化间隔重复算法。

## 7. 使用模式

### 7.1 Learn Mode（V0.1 必须）

Tutor 主导的课程学习。每次只围绕当前目标推进，并维护状态。

### 7.2 Ask Mode（V0.1 简化支持）

用户可以问当前课程相关问题。Ask 回答默认不直接改变 Mastery，除非 Tutor Engine 明确进入评估动作。

### 7.3 Lab Mode（仅占位）

路线中显示未来实验位置，但 V0.1 不连接真实集群。

## 8. 主要页面

### 8.1 Roadmap Dashboard

显示：

- 九阶段路线。
- V0.1 可学习节点与 Coming Later 节点。
- 当前节点。
- 离散掌握状态。
- “继续学习”入口。

### 8.2 Tutor Session

包含：

- 当前模块、当前知识节点、学习目标。
- Tutor 消息。
- 学生输入框和选择题组件。
- 必要时显示概念图、代码块或顺序题。
- 可折叠的“为什么这样教”调试面板。

### 8.3 Learner State

显示适合学生看的状态：

- Mastered / Partial / Learning / Review Needed。
- 当前发现的薄弱点（不展示内部数值精度）。
- 最近证据摘要。

开发模式可展示内部 mastery、confidence、misconception ID 和决策轨迹。

### 8.4 Settings / Reset

- 配置后端是否使用 Mock LLM 或真实 LLM。
- 重置默认学习者和 Golden Path 演示数据。
- API Key 不得由前端明文保存。

## 9. 核心功能需求

### FR-01 课程加载

后端能够加载、校验 `roadmap.yaml` 与 V0.1 课程文件。

### FR-02 前置关系

系统能判断节点是否 locked / ready，并找到未掌握的最近前置节点。

### FR-03 会话状态

系统保存当前节点、期待的回答类型、当前问题和 Tutor 动作。

### FR-04 回答评估

对于评估型回合，系统调用 Assessor 合同，得到严格 schema 的 JSON。

### FR-05 证据写入

每一次评估产生不可变 Evidence 记录，包含 rubric 结果、分数、提示程度和来源。

### FR-06 掌握状态更新

Tutor Engine 使用确定性规则更新 mastery / confidence / status，LLM 无权直接写最终状态。

### FR-07 误解追踪

系统能把人工定义的 misconception ID 标记为 suspected / active / resolved。

### FR-08 教学动作

V0.1 至少支持：

- `ORIENT`
- `TEACH`
- `ASK`
- `HINT`
- `ASSESS`
- `REMEDIATE`
- `RETRY`
- `REVIEW`
- `ADVANCE`
- `ANSWER_SIDE_QUESTION`

### FR-09 教师表达

Tutor Engine 确定动作后，Teacher LLM 根据动作和课程事实生成对学生的自然语言消息。

### FR-10 调试可解释性

每轮保存 Decision Trace，说明：输入证据、前置状态、候选动作、最终动作、状态变化。

### FR-11 Mock 模式

无 API Key 时仍可用固定脚本跑通 Golden Path，方便开发、测试和演示。

### FR-12 状态重置

用户可以一键恢复初始学习状态，并重新体验 Golden Path。

## 10. 非功能需求

### NFR-01 可替换模型

业务代码只能依赖 `LLMGateway` 接口，不能在 Tutor Engine 中直接调用某一家 SDK。

### NFR-02 可测试性

核心教学决策必须可以在不调用真实 LLM 的情况下测试。

### NFR-03 可追溯性

每次状态变化必须能追溯到 Evidence 与 Decision Trace。

### NFR-04 安全

- API Key 只存在后端环境变量。
- 日志不得打印完整密钥。
- 不向模型发送内部资料。
- 结构化输出必须校验，不能直接信任模型 JSON。

### NFR-05 简单优先

V0.1 优先可读、可验证的单体架构，不为未来规模提前引入复杂基础设施。

### NFR-06 中文优先

学习界面与默认课程内容使用中文；技术术语保留常见英文名称。

## 11. V0.1 成功标准

必须同时满足：

1. 本地启动后可看到完整九阶段路线。
2. 能进入 V0.1 RDMA MR 切片学习。
3. Golden Path 中错误回答会被识别为对应 misconception ID。
4. 系统会选择 REMEDIATE，而不是直接 ADVANCE。
5. 补课后状态能够变化并返回原目标节点。
6. Mastered 需要至少两种无提示证据，不能被 LLM 单次判断直接设置。
7. lkey/rkey 只在 MR 掌握后解锁。
8. 调试面板能解释每次关键决策。
9. Mock 模式自动化测试通过。
10. 真实 LLM 配置缺失或输出非法时，系统有清晰降级行为，不崩溃。

## 12. 明确留到后续版本的方向

- V0.2：扩展 Stage 1～3 更多课程、基础诊断和简单复习。
- V0.3：公开资料 RAG、引用与事实校验。
- V0.4：只读日志上传与分析练习。
- V0.5：在经过安全审批后接入隔离实验环境。
- V1.0：多人、主管视角、完整学习路线与真实任务考核。
